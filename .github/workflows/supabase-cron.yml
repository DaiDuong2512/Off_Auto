name: Supabase Cron Caller

on:
  push:               # 👉 Chạy mỗi lần bạn commit/push lên repo
    branches:
      - main          # chỉ chạy khi push vào branch main
  schedule:
    - cron: "* * * * *"   # chạy mỗi 1 phút (theo UTC)
  workflow_dispatch:        # cho phép chạy thủ công trong GitHub UI

jobs:
  call-supabase-function:
    runs-on: ubuntu-latest
    steps:
      - name: Call Supabase Edge Function
        run: |
          echo "Gọi Supabase Edge Function..."
          STATUS_CODE=$(curl -s -o response.json -w "%{http_code}" \
            -X POST "${{ secrets.SUPABASE_URL }}/functions/v1/set-stale-status-off" \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" \
            -H "Content-Type: application/json" \
            -d '{}')

          echo "HTTP status: $STATUS_CODE"
          echo "Response body:"
          cat response.json
